# 🏗️ Архитектура приложения "Convertly Hub"

Этот документ описывает высокоуровневую архитектуру SaaS-платформы "Convertly Hub", разработанную на основе [технического задания](./tech_saas.md). Архитектура спроектирована с учетом принципов масштабируемости, отказоустойчивости и удобства развертывания.

---

## 1. Общая концепция

Система построена на базе фреймворка **Next.js** и следует **трехслойной модели** с четким разделением ответственности (Separation of Concerns).

1.  **Клиентский слой (Frontend):** Пользовательский интерфейс, созданный с помощью React и Tailwind CSS. Отвечает за визуализацию, взаимодействие с пользователем и отображение данных. Рендерится преимущественно на сервере (SSR/RSC) благодаря Next.js.
2.  **Серверный слой (Backend/API):** Бизнес-логика, реализованная на базе API-маршрутов Next.js. Этот слой управляет аутентификацией, доступом к данным, валидацией и выступает в роли оркестратора, координируя работу с базой данных и ядром обработки.
3.  **Ядро обработки (Core/Workers):** Изолированная среда для выполнения ресурсоемких задач по конвертации файлов. Этот слой вынесен за пределы основного серверного процесса, чтобы избежать блокировки Event Loop.

### Схема взаимодействия компонентов

```mermaid
graph TD
    subgraph "Клиентская среда"
        A[Браузер пользователя]
        B[Стороннее приложение]
    end

    subgraph "Серверная инфраструктура"
        C(Next.js App Router)
        D(Next.js API Routes)
        E[База данных <br/>(PostgreSQL + Prisma)]
        F[Хранилище файлов <br/>(S3-совместимое)]
        
        subgraph "Ядро обработки (Workers)"
            G[Воркер "sharp" <br/>(в процессе Next.js)]
            H[Воркер "Gotenberg" <br/>(Docker-контейнер)]
        end
    end

    A -- "UI-взаимодействие (HTTP)" --> C
    A -- "Запросы (Fetch/Axios)" --> D

    B -- "Публичное API (HTTP)" --> D

    C -- "Рендеринг страниц" --> A
    
    D -- "Чтение/запись данных" --> E
    D -- "Потоковая загрузка/отдача" --> F
    D -- "Легкие задачи (JPG↔PNG)" --> G
    D -- "Тяжелые задачи (DOCX→PDF)" --> H

```

---

## 2. Структура папок проекта

Проект использует рекомендованную структуру Next.js (App Router), дополненную папками для разделения бизнес-логики.

```plaintext
/
├── 📂 app/                  # Основная папка Next.js App Router
│   ├── 📂 (auth)/           # Группа маршрутов для аутентификации
│   │   ├── 📂 login/
│   │   └── 📂 register/
│   ├── 📂 (dashboard)/      # Группа защищенных маршрутов личного кабинета
│   │   ├── 📂 dashboard/
│   │   └── 📂 docs/
│   ├── 📂 api/              # API-маршруты
│   │   ├── 📂 auth/          # Логика NextAuth.js
│   │   └── 📂 v1/
│   │       └── 📂 convert/  # Главный эндпоинт /api/v1/convert
│   ├── layout.tsx         # Корневой макет
│   └── page.tsx           # Главная страница (/)
│
├── 📂 components/           # Переиспользуемые React-компоненты (UI)
│   ├── 📂 auth/
│   ├── 📂 core/             # Базовые компоненты (кнопки, инпуты)
│   └── 📂 dashboard/
│
├── 📂 lib/                  # Вспомогательные модули и бизнес-логика
│   ├── db.ts              # Инициализация Prisma Client
│   ├── auth.ts            # Конфигурация NextAuth.js и права доступа
│   ├── s3.ts              # Логика работы с S3-хранилищем
│   └── utils.ts           # Общие хелперы
│
├── 📂 core/                 # Логика ядра обработки файлов
│   ├── image-converter.ts # Обработка изображений (sharp)
│   └── document-converter.ts# Взаимодействие с воркером Gotenberg
│
├── 📂 prisma/               # Файлы Prisma ORM
│   ├── schema.prisma      # Схема базы данных
│   └── 📂 migrations/     # Сгенерированные миграции
│
├── 📂 public/               # Статические файлы (изображения, иконки)
│
├── 📂 styles/               # Глобальные стили
│   └── globals.css
│
├── .env.local               # Переменные окружения (ключи, доступы)
├── next.config.mjs          # Конфигурация Next.js
└── package.json             # Зависимости и скрипты
```

---

## 3. Взаимодействие между частями

-   **Frontend ↔ Backend:** Клиентская часть (React-компоненты) взаимодействует с серверным слоем через Server Actions (для форм) и прямые API-вызовы к маршрутам в `/app/api`. Аутентификация сессии пользователя управляется библиотекой **NextAuth.js** через защищенные `HttpOnly` cookie.

-   **Backend ↔ База данных:** Весь доступ к PostgreSQL осуществляется через **Prisma ORM**. `Prisma Client` инициализируется в `lib/db.ts` и используется во всех API-маршрутах для CRUD-операций с пользователями, API-ключами и историей конвертаций.

-   **Backend ↔ Хранилище S3:** Для загрузки и скачивания файлов бэкенд взаимодействует с S3-совместимым хранилищем. Он генерирует временные подписанные URL (presigned URLs) для безопасной прямой загрузки файлов с клиента в S3, минимизируя нагрузку на сервер.

-   **Backend ↔ Ядро (Workers):**
    -   При конвертации изображений API-маршрут напрямую вызывает функцию из `core/image-converter.ts`, которая использует библиотеку `sharp`.
    -   При конвертации документов API-маршрут отправляет файл по сети (внутренний HTTP-запрос) на адрес Docker-контейнера, где запущен **Gotenberg**. После обработки он получает результат и возвращает его клиенту.

---

## 4. Развертывание (Deployment)

Предложенная архитектура упрощает процесс развертывания и масштабирования.

-   **Основное приложение (Next.js):** Может быть развернуто как единое приложение на платформах вроде **Vercel** (рекомендуется), Netlify или на собственном сервере с Node.js (например, в Docker-контейнере).
-   **База данных (PostgreSQL):** Рекомендуется использовать управляемый сервис, такой как **Supabase**, **Neon**, **Railway** или **AWS RDS**, чтобы делегировать задачи резервного копирования и масштабирования.
-   **Хранилище файлов (S3):** Используется внешний сервис. Для старта идеально подходит **Cloudflare R2** из-за отсутствия платы за трафик.
-   **Воркер (Gotenberg):** Разворачивается как отдельный **Docker-контейнер**. Его можно запустить на том же сервере, что и Next.js-приложение (для простоты), или на отдельной виртуальной машине для независимого масштабирования.

Такое разделение позволяет масштабировать каждый компонент независимо. Например, если конвертация документов создает высокую нагрузку, можно увеличить количество контейнеров `Gotenberg`, не затрагивая основное приложение.
