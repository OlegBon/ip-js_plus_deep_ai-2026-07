# План выполнения работ: Convertly Hub

Этот план составлен на основе технического задания и архитектурной документации.

## Незавершённые задачи — приоритетный обзор

Этот раздел дублирует все незакрытые пункты из тематических разделов ниже, чтобы следующий этап работы был виден сразу. Канонические чекбоксы остаются в исходных разделах; после выполнения обновляются оба места.

Обозначения: `+` — задачу можно выполнять уже сейчас; `-` — пока не берём в работу. Цифра — общий приоритет, где `1` самый высокий.

1. **(-, 10) Платежи и тарифы — заморожено до выбора провайдера**
   - **Почему не завершено:** тарифы и `PaymentModal` пока являются демонстрационным UI; не выбран провайдер и поэтому отсутствуют checkout, проверка webhook, идемпотентная синхронизация подписки и управление доступом.
   - **Как выполнить:** после выбора провайдера определить модель подписок и страны/валюты, реализовать checkout, customer portal, верификацию и идемпотентную обработку webhook, затем связать тариф с правами пользователя и добавить контрактные тесты.
   - **Когда:** намеренно отложено; не входит в ближайший локальный MVP и возобновляется только после продуктового решения о провайдере.

2. **(+, 1) Обновление уязвимых production-зависимостей**
   - **Почему не завершено:** `npm audit --omit=dev` сообщает о пяти уязвимостях (четыре high, одна moderate) в транзитивных цепочках Prisma и NextAuth/Nodemailer. Автоматический fix требует breaking-обновлений.
   - **Как выполнить:** изучить release notes и migration guides, обновить Prisma Client/CLI и зависимости аутентификации контролируемо, проверить lockfile, миграции, auth/email flows, полный тестовый набор и build. Не применять `npm audit fix --force` без ревью диффа.
   - **Когда:** сразу после стабилизации MVP и до любого production deployment.

3. **(-, 9) Backend‑5 — `PDF → DOCX`**
   - **Почему не завершено:** PDF обычно не содержит полной структуры исходного документа, а Gotenberg не даёт качественную обратную конвертацию; текущий Core поддерживает только `JPG ↔ PNG` и `DOCX → PDF`.
   - **Как выполнить:** выбрать специализированный best-effort движок, подготовить эталонные PDF/DOCX и критерии приемлемой потери форматирования, добавить очередь/таймауты и отдельные quality-тесты.
   - **Когда:** после подтверждения бизнес-ценности направления и выбора движка; не блокирует текущий MVP.

4. **(-, 8) Backend‑8 — общий rate limiter для нескольких инстансов**
   - **Почему не завершено:** текущий лимит 30 запросов в минуту на API-ключ хранится в памяти одного процесса; при нескольких экземплярах приложения лимиты будут независимыми.
   - **Как выполнить:** подключить Redis-совместимое хранилище, применить атомарный sliding/fixed-window алгоритм, добавить health-check, метрики и интеграционные тесты конкурирующих запросов.
   - **Когда:** до горизонтального масштабирования или production-развёртывания с несколькими репликами Next.js.

5. **(-, 7) Backend‑9 — производительность поиска админ-панели на большой БД**
   - **Почему не завершено:** cursor-pagination уже исключает большой `OFFSET`, но поиск подстрокой по имени/email через `contains` не имеет специализированного PostgreSQL-индекса.
   - **Как выполнить:** снять `EXPLAIN (ANALYZE, BUFFERS)` на реалистичных данных, при необходимости добавить миграцию с `pg_trgm` и GIN-индексами, затем сравнить план и время ответа.
   - **Когда:** при росте таблицы пользователей или перед production-нагрузкой; не требуется для текущего MVP-объёма данных.

6. **(+, 2) Прочие задачи‑2 — реальные backend integration/E2E тесты**
   - **Почему не завершено:** 106 unit-, component- и route-тестов покрывают серверную логику с изоляцией зависимостей, а UI E2E-сценарии существуют; но нет полноценного E2E-набора, который через HTTP использует реальную PostgreSQL, MinIO и Gotenberg для auth, API-ключей, конвертации и admin API.
   - **Как выполнить:** выделить изолированную тестовую БД/бакет, seed-пользователей и ключей, расширить Playwright/API tests критическими сценариями и очищать данные после запуска.
   - **Когда:** перед staging/production и после подключения Dashboard к реальным backend-контрактам.

7. **(-, 8) Полноценное восстановление пароля через Telegram**
   - **Почему не завершено:** текущая привязка хранит только защищённый chat ID после `/start link_…`; пароль можно восстановить по email, а ввод `@username` лишь валидируется. У бота нет сохранённого username и нет исходящей доставки reset-ссылки, поэтому нельзя создавать видимость работающего Telegram-reset.
   - **Как выполнить:** при webhook-привязке безопасно сохранять нормализованный публичный Telegram username (если он есть), сопоставлять только подтверждённый аккаунт, создавать уже существующий одноразовый хешированный токен и отправлять ссылку в связанный chat через Bot API. Нужны нейтральные ответы против user enumeration, общий rate limit, сценарий отсутствующего username, тесты webhook/Bot API и отдельные integration/E2E-проверки.
   - **Когда:** после согласования Telegram как канала восстановления и до production-релиза; не блокирует текущий локальный MVP, где надёжным каналом остаётся email.

8. **(-, 10) Прочие задачи‑3 — production deployment**
   - **Почему не завершено:** локальный Docker Compose проверен, но нет целевой платформы, production-конфигурации, CI/CD и эксплуатационной процедуры.
   - **Как выполнить:** сравнить Vercel, Render и Oracle Cloud Free Tier по региону, стоимости, лимитам, managed PostgreSQL/S3/Redis и возможности постоянно запускать Gotenberg. Vercel удобен для Next.js, но Gotenberg и постоянное файловое хранилище нужно вынести во внешние сервисы; Render позволяет развернуть web и worker-сервисы, но бесплатные лимиты и сон сервисов непригодны для стабильной обработки; Oracle Free Tier может вместить Docker Compose на VM, но требует самостоятельного администрирования, backup и проверки доступности выбранного региона. После выбора: настроить секреты, миграции, HTTPS/домен, backup/restore, мониторинг, CI/CD и runbook.
   - **Когда:** после закрытия обязательных production-пунктов выше и до первого публичного релиза.

## Результаты аудита: задачи для локального MVP без оплаты

Эти задачи выявлены при сверке экранов, API, Prisma-схемы, документации и production-сборки. Они намеренно не дублируются в приоритетном обзоре выше: тот блок оставляет только шесть согласованных отложенных направлений.

1. **Локальный старт и актуальная документация** (выполнено)
   - [x] Добавлен безопасный `.env.example`; `NEXTAUTH_URL` — единственный публичный origin для NextAuth и ссылок email, а Docker-адреса явно отделены как внутренние.
   - [x] Создана каноническая инструкция `docs/local-start.md`: preflight, Compose, миграции, проверки, регистрация, первый администратор, Telegram и диагностика.
   - [x] README, START, карта структуры и публичные примеры API синхронизированы с фактическим локальным MVP.

2. **Реальная конвертация из браузера для авторизованного пользователя** (выполнено)
   - [x] Добавить session-защищённый `POST /api/account/conversions`, повторно использующий валидацию, Core, тарифные квоты и правила приватности без передачи API-ключа в браузер.
   - [x] Подключить загрузку на главной странице для залогиненного пользователя: `JPG ↔ PNG` и `DOCX → PDF`; при выключенном хранении отдавать поток на скачивание, а при включённом — показывать постановку в очередь и историю Dashboard.
   - [x] Ограничить browser-документы до фактически поддерживаемого `DOCX → PDF`; гостевой режим ведётся отдельным публичным endpoint.

3. **Dashboard на реальных данных** (выполнено)
   - [x] Подключить профиль, тариф/usage, API-ключи, `storeConversions`, Telegram и историю к session-защищённым account API.
   - [x] Убрать фиктивную генерацию ключа: секрет показывается только после `POST`, ключи отзываются owner-scoped endpoint.
   - [x] Убрать неподдерживаемые mock-действия профиля; разрешено только безопасное изменение отображаемого имени.

4. **Админ-панель и мониторинг на реальных данных** (выполнено)
   - [x] `UserManagement` использует защищённые admin API, поддерживает поиск, cursor-загрузку, блокировку/разблокировку и отзыв активных API-ключей; demo edit/delete удалены.
   - [x] `SystemMonitoring` получает защищённые фактические метрики пользователей/конвертаций/ошибок и статусы PostgreSQL, MinIO и Gotenberg.
   - [x] `/api/health` использует `GOTENBERG_URL`, проверяет S3 и не раскрывает публично внутренние счётчики.
   - [ ] При появлении админской истории конвертаций: добавить отдельные `ADMIN` API для поиска/сортировки и массового удаления результатов; после второго использования выделить общие нейтральные list/table-части с Dashboard, оставив выбор строк и destructive-действия в admin-container.

5. **Восстановление пароля и подтверждения без оплаты:** (выполнено)

- [x] Реализованы одноразовые хешированные reset- и email-verification-токены с TTL, нейтральный ответ против user enumeration, смена пароля и локальное ограничение частоты.
- [x] Добавлены MailHog для локальной доставки и настраиваемый SMTP-транспорт для production; Telegram-подтверждение использует уже реализованную webhook-привязку.
- [x] UI восстановления и профиля подключён к реальным API и показывает фактический статус email/Telegram.

6. **Гостевая конвертация без регистрации** (выполнено)
   - [x] Добавлен публичный поток без API-ключа и S3: `JPG ↔ PNG` до 3 раз и `DOCX → PDF` до 2 раз за месяц, исходный файл до 1 МБ.
   - [x] Квота привязана к хешу случайной `HttpOnly` cookie; результат возвращается потоком без серверной истории и хранилища.
   - [x] Добавлены IP limiter, остаток и дата сброса квоты, раздельное отключение upload-зон и локальный список результатов: browser IndexedDB хранит файлы только 10 минут, затем удаляет их; S3 и БД не используются.

7. **Стабилизация локального MVP: квоты, регрессии и качество кода** (выполнено)
   - [x] Исправлены ссылки локальной инструкции, актуализированы browser/guest тексты и Playwright flow для выбора тарифа гостем.
   - [x] Главная страница гидратирует фактические guest-квоты; guest, месячная conversion- и storage-квоты резервируются атомарно на сервере.
   - [x] Устранены все ESLint errors без отключения правил; добавлены unit-проверки гидратации guest-квоты.

## Frontend

> Статус «выполнено» в этом разделе означает, что экран и его клиентское UX-поведение реализованы. Часть экранов всё ещё использует демонстрационные данные, пока соответствующий UI не будет подключён к уже существующему или будущему backend-контракту.

1.  **Настройка проекта:** (выполнено)
    - [x] Инициализировать Next.js (App Router) с Tailwind CSS.
2.  **Основной макет:** (выполнено)
    - [x] Создать корневой layout (`app/layout.tsx`) с навигацией и футером.
3.  **Главная страница (`/`):** (выполнено)
    - [x] Реализовать два виджета Drag & Drop для конвертации изображений и документов, включая индикаторы загрузки и состояния.
4.  **Аутентификация (`/auth/...`):** (выполнено)
    - [x] Создать страницы и формы для регистрации (`/register`) и входа (`/login`).
5.  **Личный кабинет (`/dashboard`):** (выполнено)
    - [x] Разработать компонент для управления профилем пользователя.
    - [x] Реализовать секцию для генерации, копирования и регенерации API-ключа.
    - [x] Добавить переключатель для управления настройками приватности (сохранение файлов).
    - [x] Создать таблицу для отображения истории конвертаций (файловый менеджер).
6.  **Стилизация страницы 404:** (выполнено)
    - [x] Создать дизайн для страницы 404 в стиле всего проекта.
7.  **Прайс (/pricing):** (выполнено)
    - [x] Создать страницу с тарифами.
8.  **Документация API (`/docs`):** (выполнено)
    - [x] Создать статическую страницу с описанием эндпоинтов и примерами кода.
9.  **Панель администратора:** (выполнено)
    - [x] Спроектировать и реализовать интерфейс для управления пользователями и мониторинга системы.
10. **UI-компоненты:** (выполнено)
    - [x] Создать переиспользуемые компоненты для уведомлений (Toasts), модальных окон и других элементов обратной связи.
11. **Восстановление доступа и подтверждения:** (выполнено)
    - [x] Создать страницы и клиентские формы восстановления пароля по email/Telegram, включая динамическую кнопку «Назад».
    - [x] Добавить UI статусов и действий для верификации email и Telegram в профиле.
    - [x] Реализовать отправку и одноразовую проверку токенов, фактический сброс пароля, подтверждение email через SMTP/MailHog и реальные статусы профиля.
12. **Платежи и тарифы:** (заморожено до выбора провайдера)
    - [x] Создать модальное окно для выбора тарифа и имитации начала оплаты.
    - [x] Реализовать каталог Convertly-тарифов, server-side квоты, Free с временным хранением, отображение срока жизни файлов и Mock Checkout с сохранением единственной ожидающей смены тарифа.
    - [ ] Выбрать провайдера и реализовать checkout, webhook-верификацию и синхронизацию тарифа; подробный следующий этап указан в приоритетном обзоре выше.
13. **Гостевая конвертация без регистрации:** (выполнено)
    - [x] На главной странице показаны английское пояснение о пробном режиме, активные квоты и доступные 10 минут результаты в текущей вкладке.
    - [x] При исчерпании квоты приглушается только соответствующий upload-блок; blocking auth-modal не используется, регистрация предлагается через CTA.

## Backend

> Статус «выполнено» в этом разделе означает, что server-side контракт, авторизация, валидация, обработка ошибок и релевантные автоматические тесты реализованы для заявленного MVP-сценария. Это не означает готовность к любому production-масштабу: отдельные явно отмеченные пункты могут требовать внешнего провайдера, общего Redis-хранилища, специализированного движка или нагрузочной проверки.

1.  **Аутентификация пользователей:** (выполнено)
    - [x] Настроить `NextAuth.js` для управления JWT-сессиями через HttpOnly cookies и защитить маршруты личного кабинета.
2.  **Логика регистрации/входа:** (выполнено)
    - [x] Реализовать хэширование паролей (`bcrypt`), регистрацию, проверку учётных данных и вход через Credentials Provider.
3.  **Роли пользователей и Telegram:** (выполнено)
    - [x] Реализовать серверную RBAC-проверку: `USER` и `ADMIN` имеют доступ к Dashboard, а `/management` — только `ADMIN`.
    - [x] Добавить безопасный процесс назначения первого администратора и аудита последующих изменений роли.
    - [x] Реализовать привязку Telegram через одноразовый токен, верификацию webhook и подтверждение `telegramId` в профиле.
    - [ ] Полноценное восстановление пароля через Telegram (см. приоритетный обзор): сохранять публичный username при привязке, отправлять одноразовую ссылку через Bot API в подтверждённый chat и покрыть поток rate-limit/integration-тестами.
4.  **API для конвертации (`POST /api/v1/convert`):** (выполнено)
    - [x] Создать основной маршрут для приёма `multipart/form-data` запросов.
    - [x] Реализовать аутентификацию по Bearer токену (API-ключ).
    - [x] Валидировать поддерживаемые MIME-типы, целевой формат и размер файла до 10 МБ; создавать задачу `PENDING` и возвращать `202 Accepted`.
5.  **Ядро конвертации (Core):** (выполнено для текущего MVP)
    - [x] Интегрировать библиотеку `sharp` для быстрой обработки изображений (JPG ↔ PNG).
    - [x] Реализовать логику делегирования конвертации документов (DOCX → PDF) воркеру `Gotenberg`.
    - [x] Добавить асинхронный жизненный цикл задания `PENDING → PROCESSING → COMPLETED/FAILED`, проверку сигнатуры исходного файла и безопасное фиксирование метаданных результата.
    - [ ] `PDF → DOCX` остаётся planned: потребуется специализированный best-effort-движок, эталонные тестовые документы и предупреждение о возможной потере форматирования.
6.  **Управление приватностью:** (выполнено)
    - [x] Если `storeConversions=true`, сохранять готовый файл в приватном S3 по ключу пользователя и выдавать его только через авторизованный download-маршрут.
    - [x] Если `storeConversions=false`, не записывать файл в S3 и отдавать результат конвертации бинарным потоком с `Cache-Control: no-store`.
7.  **Генерация API-ключей:** (выполнено)
    - [x] Добавить session-защищённые API для просмотра, создания и отзыва собственных API-ключей.
    - [x] Генерировать криптографически стойкий секрет, возвращать его только один раз и хранить в PostgreSQL исключительно SHA-256-хеш.
    - [x] Выполнять отзыв атомарно только для активного ключа текущего пользователя.
8.  **Безопасность и валидация:** (выполнено для текущего MVP)
    - [x] Реализовать ограничение частоты запросов: 30 запросов в минуту на действующий API-ключ, с `429 Too Many Requests` и `Retry-After`.
    - [x] Добавить серверную валидацию размера (`413 Payload Too Large`), типа (`415 Unsupported Media Type`) и целевого формата (`422 Unprocessable Entity`) до чтения файла в `Buffer`; Core дополнительно проверяет сигнатуру.
    - [x] Обеспечить безопасную обработку ошибок Core/воркеров без раскрытия внутренних деталей (`422 Unprocessable Entity`).
    - [ ] Перед горизонтальным масштабированием заменить in-memory limiter на общий Redis-совместимый backend.
9.  **Логика для Админ-панели:** (выполнено для текущего MVP)
    - [x] Добавить server-side защищённый `ADMIN` API для cursor-поиска пользователей по имени и email.
    - [x] Реализовать блокировку и разблокировку пользователя через статусы `SUSPENDED` / `ACTIVE`, с запретом смены собственного статуса.
    - [x] Реализовать отзыв любого активного API-ключа администратором без удаления истории конвертаций.
    - [ ] Перед ростом таблицы пользователей проверить планы запросов и добавить индекс `pg_trgm` для поиска подстрокой по имени/email.

## База данных и Инфраструктура

> Статус «выполнено» в этом разделе означает, что инфраструктурный компонент или серверный модуль реализован и проверен в локальном Docker-окружении. Это само по себе не означает, что пользовательский API или поток конвертации уже подключён к нему.

1.  **Локальное окружение:** (выполнено)
    - [x] Настроены сервисы PostgreSQL, MinIO и Gotenberg в `docker-compose.yml`.
    - [x] Сервисы запущены и проверены: PostgreSQL принимает подключения, MinIO отвечает на liveness endpoint, Gotenberg сообщает готовность Chromium и LibreOffice.
2.  **Схема данных:** (выполнено)
    - [x] Добавлены модели `User`, `ApiKey` и `ConversionLog` с индексами и связями.
    - [x] Добавлены роли, статус блокировки, тариф, настройка хранения, метаданные результатов и статусы конвертаций.
    - [x] Добавлено хранение только хешей API-ключей и одноразовых auth-токенов.
    - [x] Схема валидирована Prisma и зафиксирована миграцией `20260822120000_finalize_schema`.
3.  **Миграции:** (выполнено)
    - [x] В репозитории есть начальная миграция `20260816094436_auth_features`.
    - [x] Добавлена и применена миграция `20260822120000_finalize_schema`; `npx prisma migrate status` подтверждает актуальность локальной базы.
4.  **Интеграция с S3:** (выполнено)
    - [x] Добавлен серверный S3-сервис для загрузки, скачивания и удаления файлов через MinIO.
    - [x] Добавлена проверка доступности бакета и защита от пустых или абсолютных ключей объектов.
    - [x] Операции upload/download/delete проверены на локальном MinIO.

## Прочие задачи

> Статус «выполнено» в этом разделе означает, что указанная инженерная практика или инструмент настроены и проверены в текущем репозитории. Он не означает завершение всех связанных продуктовых или backend-сценариев.

1.  **Качество кода:** (выполнено)
    - [x] Настроить ESLint и Prettier для автоматического форматирования и линтинга.
2.  **Тестирование:** (частично выполнено)
    - [x] Добавлены unit-, component- и integration-тесты frontend для загрузки файлов, форм аутентификации, модальных окон, оплаты, поиска, пагинации, Dashboard и управления пользователями.
    - [x] Настроены Jest, React Testing Library, `@testing-library/user-event`, MSW и команды `npm test` / `npm run test:coverage`.
    - [x] Настроены Playwright и Chromium, добавлены команды `npm run test:e2e` / `npm run test:e2e:ui` и E2E-сценарии для главной страницы, входа, выбора тарифа, Dashboard и Admin.
    - [x] Добавлены backend unit- и route-тесты: API-аутентификация, API-ключи, Core-конвертация, приватность результатов, rate limiting и admin API.
    - [ ] Добавить реальные integration/E2E-тесты backend через HTTP с изолированными PostgreSQL, MinIO и Gotenberg; подробный следующий этап указан в приоритетном обзоре выше.
3.  **Развертывание:** (не выполнено)
    - [ ] Подготовить и сравнить варианты production-развёртывания: Vercel с внешними managed-сервисами, Render с отдельными web/worker-сервисами и Oracle Cloud Free Tier с самостоятельно администрируемым Docker Compose.
    - [ ] После выбора платформы подготовить скрипты и инструкции для Next.js, PostgreSQL, S3, Redis и Gotenberg; настроить секреты, миграции, домен/HTTPS, backup/restore, мониторинг и CI/CD. Подробный следующий этап указан в приоритетном обзоре выше.
